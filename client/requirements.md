## Packages
framer-motion | Smooth layout transitions and entry animations for the dashboard cards
clsx | Utility for conditional class names (standard with shadcn)
tailwind-merge | Utility for merging tailwind classes (standard with shadcn)

## Notes
- The app uses a dark theme by default (slate-950 background)
- API endpoints are read-only (GET) for this dashboard view
- Icons provided by lucide-react
